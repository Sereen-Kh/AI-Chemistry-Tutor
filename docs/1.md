You are a senior backend architect and AI/RAG code auditor.

I need you to deeply inspect the EduMind backend codebase and verify the real implemented state of all AI APIs, RAG APIs, ingestion APIs, database models, service layers, and missing implementation gaps.

Context:
This project is an AI Chemistry Tutor backend using FastAPI, SQLAlchemy, PostgreSQL/pgvector, Redis, Celery, Gemini AI, OCR/document understanding, and RAG retrieval.

Important terminology:

- “RAG system” means retrieval-augmented generation.
- Do not assume the architecture document is 100% correct.
- The goal is to compare the documented architecture against the actual codebase.

Your task has 6 phases.

PHASE 1 — Repository scan
Scan the full backend codebase and identify:

1. All API route files.
2. All service files related to AI, RAG, OCR, ingestion, embeddings, homework, quiz, flashcards, study plan, and chat.
3. All SQLAlchemy models related to:
   - ContentSource
   - RagChunk
   - TextbookChunk
   - ExtractedQuestion
   - IngestionJob
   - IngestionPage
   - ChatSession
   - ChatMessage
   - Homework
   - Question / Quiz / Flashcard models
4. All schemas/Pydantic models used by these APIs.
5. All configuration keys related to Gemini, embeddings, OCR, RAG, Redis, Celery, and pgvector.

PHASE 2 — AI external API audit
Find every external AI API call in the codebase.

For each external AI call, document:

- File path
- Function/class name
- Model name used
- API method used, for example:
  - generateContent
  - embedContent
  - files.upload
- Input shape
- Output shape
- Whether it is synchronous or wrapped with asyncio.to_thread
- Error handling behavior
- Fallback behavior
- Whether response validation exists
- Whether retry logic exists
- Whether structured JSON mode / response schema is used

Specifically verify:

1. Tutor/chat generation model.
2. Embedding model.
3. OCR/document understanding model.
4. OCR fallback model.
5. Quiz generation model.
6. Flashcard generation model.
7. Study plan generation model.
8. Homework solver model.
9. Any local/deterministic fallback.

PHASE 3 — RAG system audit
Inspect the complete RAG system and document the real implementation.

Verify:

1. How user query embedding is generated.
2. Whether document embeddings and query embeddings use different task types.
3. Whether pgvector is used.
4. Exact vector column dimension.
5. Exact similarity method: cosine distance, L2, inner product, etc.
6. Whether hybrid scoring exists.
7. Exact vector/lexical score weights.
8. Arabic normalization logic:
   - diacritics removal
   - alef normalization
   - yaa/taa normalization
   - stopword removal
   - stemming
   - term expansion
9. Redis cache behavior:
   - key format
   - TTL
   - what is cached
10. Fallback behavior when PostgreSQL is not used.
11. Confidence thresholds.
12. What happens when no relevant chunks are found.
13. Whether source page numbers are returned.
14. Whether retrieved chunks are traceable to ContentSource / page / lesson / topic.

PHASE 4 — API implementation status
Create a complete table of all AI/RAG-related backend APIs.

Separate them into these groups:

A. Implemented AI APIs

- Chat session messages
- Stateless RAG ask
- Homework solve text
- Homework solve image
- Quiz generation
- Flashcard generation
- Study plan generation
- Any other AI endpoint found

B. Implemented RAG APIs

- Retrieve
- Search
- Source list
- Stats
- Test query
- Any admin/debug RAG endpoint found

C. Implemented ingestion/admin APIs

- Start ingestion
- Ingestion status
- Retry page
- Review extracted questions
- Clear/reindex source
- Any other ingestion endpoint found

D. Missing but recommended APIs
Based on the current codebase, propose APIs that are missing but important for production, especially:

- Upload PDF/source file API
- RAG debug endpoint showing vector score, lexical score, final score
- RAG evaluation endpoint with expected answer and source coverage
- Chunk review/edit/delete endpoint
- Extracted question approve/reject endpoint
- Chemistry dictionary/knowledge-base API
- Reindex source/chapter/topic endpoint
- Cache clear endpoint
- AI usage/cost/latency monitoring endpoint
- User feedback endpoint for bad answers
- Streaming chat endpoint
- Source citation verification endpoint

For every API, provide:

- Method
- Path
- Auth requirement
- Request JSON
- Response JSON
- Current status: Implemented / Partial / Missing
- File path if implemented
- Recommended priority: P0 / P1 / P2

PHASE 5 — Gap and risk analysis
Identify problems in the current implementation.

Focus on:

1. Any documented API that does not exist in code.
2. Any route that exists but is incomplete.
3. Any service function that is unused.
4. Any schema mismatch between API and service.
5. Any database model mismatch.
6. Any missing migration for pgvector or AI/RAG tables.
7. Any missing indexes.
8. Any hardcoded AI model names.
9. Any missing environment variables.
10. Any weak error handling.
11. Any malformed JSON risk from Gemini outputs.
12. Any missing retry/backoff logic.
13. Any OCR quality-gate weakness.
14. Any Arabic retrieval weakness.
15. Any place where hallucination risk is high.
16. Any place where the system answers without enough source grounding.
17. Any place where source citations/page references are missing.

PHASE 6 — Output format
Return the result as a structured audit document.

Use this exact structure:

# EduMind AI/RAG Codebase Audit

## 1. Executive Summary

Short summary of what is implemented, what is partial, and what is missing.

## 2. External AI API Call Map

Table with:

- Service
- File
- Function
- Model
- API method
- Input
- Output
- Validation
- Fallback
- Risk

## 3. Implemented AI/RAG Backend APIs

Table with:

- Method
- Path
- Purpose
- Auth
- Request schema
- Response schema
- File path
- Status

## 4. RAG Retrieval Implementation

Explain the actual retrieval flow found in code.
Include exact scoring formula if present.

## 5. OCR / Document Ingestion Implementation

Explain the real ingestion flow found in code.
Include PDF upload, page classification, Gemini extraction, image fallback, chunking, embedding, and DB inserts.

## 6. Database Model Verification

Table with:

- Model
- Table
- Important columns
- Relationships
- Indexes
- Issues found

## 7. Missing APIs to Implement

Prioritized table:

- Priority
- API name
- Method/path
- Why needed
- Suggested request JSON
- Suggested response JSON

## 8. Bugs / Risks / Inconsistencies

List concrete issues only.
For every issue, include:

- Severity: Critical / High / Medium / Low
- File path
- Evidence
- Recommended fix

## 9. Recommended Implementation Plan

Give a step-by-step plan:
P0: must fix before production
P1: important for reliable RAG
P2: improvement / observability

## 10. Exact Files to Modify

List files that should be created or changed.

Rules:

- Do not guess. If something is not found, write “Not found in codebase”.
- Always cite exact file paths and function/class names.
- Include line numbers if your tool supports them.
- Do not rewrite code yet unless I explicitly ask.
- Do not make cosmetic comments.
- Focus only on backend AI, RAG, OCR, ingestion, and related APIs.
